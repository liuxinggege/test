{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "type": "shell",
            "command": "rm -rf ./bin && tsc && cd ..",
            "label": "clean bin then tsc",
            "identifier": "clean_bin_and_tsc",
        }
    ]
}